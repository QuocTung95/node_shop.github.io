const { isValidName, isValidEmail } = require('./validation');
// const { isValidName, isValidEmail } = require('./dateUtils');
// const { isValidName, isValidEmail } = require('./stringUtils');

module.exports = {
  isValidName,
  isValidEmail
}
